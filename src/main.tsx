import React from 'react'
import ReactDOM from 'react-dom/client'
import 반짝반짝테크데이퀘스트 from './App'
import '@/styles/index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <반짝반짝테크데이퀘스트 />
  </React.StrictMode>,
)